apiVersion: controlplane.epydios.ai/v1alpha1
kind: ExtensionProvider
metadata:
  name: negative-bearer-missing-secret
spec:
  providerType: ProfileResolver
  providerId: negative-bearer-missing-secret
  contractVersion: v1alpha1
  endpoint:
    url: http://oss-profile-static-resolver.epydios-system.svc.cluster.local:8080
    timeoutSeconds: 5
  auth:
    mode: BearerTokenSecret
    bearerTokenSecretRef:
      name: negative-provider-auth-missing
      key: token
  selection:
    enabled: false
    priority: 1
  advertisedCapabilities:
    - profile.resolve


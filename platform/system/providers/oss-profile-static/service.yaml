apiVersion: v1
kind: Service
metadata:
  name: oss-profile-static-resolver
spec:
  selector:
    app.kubernetes.io/name: oss-profile-static-resolver
    app.kubernetes.io/component: provider
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP

